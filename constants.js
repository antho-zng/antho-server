const PORT = 4000;
const HOMEPAGE_MESSAGE = "this is antho-server :-) !"; //TODO: messaging that instructs users to make set or get requests
const REQUEST_ACTIONS = {
  GET: "/get",
  SET: "/set",
};

module.exports = { PORT, HOMEPAGE_MESSAGE, REQUEST_ACTIONS };
